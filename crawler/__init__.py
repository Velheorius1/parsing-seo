"""Пакет краулера.

Единственное, что здесь происходит при импорте, — установка редакции секретов в
логах. Место выбрано потому, что это единственная точка, через которую проходит
ЛЮБОЙ импорт любого модуля пакета: `basicConfig` раскидан по двум десяткам
скриптов и вызывается то до, то после импортов, поэтому защита, привязанная к
обработчикам логгера, срабатывала бы через раз. Подробности — в
`crawler/core/log_redact.py`.
"""
try:
    from crawler.core.log_redact import install as _install_log_redaction

    _install_log_redaction()
except Exception:      # pragma: no cover — логи важны, но не ценой импорта пакета
    pass
