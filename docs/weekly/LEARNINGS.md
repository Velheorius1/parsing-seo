# Parsing-SEO — журнал непрерывного обучения (append-only)

Одна строка в неделю. Ведёт еженедельная рутина (см. ROUTINE.md). Цель — видеть траекторию оценки и какие ошибки недели превращены в улучшения. НЕ редактировать прошлые строки.

| Неделя | Score (Δ) | Главная находка недели | Что применено | Prune-кандидаты (report-only) |
|--------|-----------|------------------------|---------------|-------------------------------|
| 2026-W25 (06-14→06-21) | **6.6** (baseline) | UZEX deep-ссылки сломаны с 21.05 (prequest→удалённая «Лопата» 2021, Э-магазин→«no data found») = главный потолок (link 50%). Доп: 0 фидбека за 30д; AI p95 ×2.6 (5.98→15.9с). | Починены 6 шаблонов (prequest→proposal-request, auctions→auction/detail, Э-магазин×4→new-xarid); метка «🔄 Обратный тендер»; healthcheck 4×→1×/день. Проектно link 50→88%, score→~7.6. | 23 источника мертвы ≥3 нед (Cooperation print-подфиды, сезонные банки). Не отключены. |
| 2026-W26 (06-15→06-22) | **7.2** (+0.6 vs W25) | Scheduled-прогон 06-22 **измерил precision AI-судьёй = 7.0** (baseline-черновик 06-21 брал прокси 8.0 → завышал композит до 7.65). 3 FP из 10 — все из TG-каналов клиентских запросов: голое приветствие + 2 рекламы поставщиков. Браузер 6/8 valid_lot → link 7.5 (оба Cooperation `?lotId=SL`=404, но это известный mitigated-кейс, не регрессия). AI p95 всё ещё ×2.65 (5.98→15.1с) при +31% объёма — провайдерская очередь, не код. | Код не трогал (фиксы 06-21 живы). Авторитетный композит = **7.2 измеренный** (не 7.65 прокси). Идея прогона: pre-filter шума TG-каналов (приветствия+реклама поставщиков), цель precision ≥8.5 — ОПИСАНА Данияру, требует теста на 7д. | Те же 23 (топ: Минстрой 440, Cooperation Bosma 993). Не отключены. |
| 2026-W29 (07-07→07-13) | **7.5** (−0.2 vs W28) | Δ −0.2 = **измерит. шум, не регрессия**: cost −1.1 (p95 3.66→8.74с, err 0.4%) на низком объёме (252 AI-вызова) + platform −0.4 (dead 26→31, транзиентные). AI-calls −80% (1250→252) = инфляция W28 (бэкфилл prequal-фикса), а не поломка: ингест здоров (27.4к/2д), алертов 708/7д, relevance-rate ВЫРОСЛА 17→29%. **НОВОЕ:** Cooperation.uz `?lotId=SL` deep-ссылки мертвы (обе→«топилмади»), но источник уже dead/prune → low impact. Браузер 5/7 valid → link 7.1. Precision measured n=156: 34 FP→78.2%→7.8. | **Ничего (report-only).** Осознанный отказ от костылей: находки = link-шаблон мёртвого источника (не чинить спекулятивно) + scoring-правки, требующие теста. Recall-аудит 6 авто-мьютов CLEAN (все OFF-scope). | Те же 23 (Ebirja Нац.магазин 1221, Cooperation Bosma 993, Минстрой 440). Не отключены. |
| 2026-W28 (06-30→07-06) | **7.7** (+0.5 vs W26; W27 не прогонялся) | **prequal-ссылки поломаны в проде вопреки фиксу 06-21:** `fetch_uzex_auctions.py:246` хардкодил `xarid.uzex.uz/prequalification/detail/{id}` (→homepage-redirect) в обход исправленного `sources.yaml`. Браузер 6/9 valid → link 6.7. Precision впервые **measured на реальном фидбеке n=209** (не прокси): 41 FP → 80.4% → 8.0. AI p95 регрессия **снята**: 5.9→3.66с (−38%), err 0% → cost 10.0. | **Фикс prequal применён** (браузер-верифицирован: new-xarid proposal-request/detail/83758 = valid lot), commit+push main. Recall-аудит: активный мьют `TG PR Media Group` (neg11) прячет реальный лид (6-11к флаеров) — НЕ снят авто (explicit neg Данияра), вынесен на решение. Pre-filter TG-шума — снова описан (теперь с доказательством: 41 FP ≈ 20%, домин. supplier-ads/техника). | Те же 26 (топ: Ebirja Нац.магазин 1221, Cooperation Bosma 993, Минстрой 440). Не отключены. |
| 2026-W30 (07-14→07-20) | **7.6** (+0.1 vs W29) | **Обвал precision 7.8→1.5 = смена определения FP, НЕ регрессия.** Коммит `3e0a2cf` (07-16, mid-W29) переопределил `false_positives` в `feedback.py`: было `original_label in (demand,customer_request) AND corrected=ad` (почти никогда не срабатывало — `original_label` хранил `message_type`), стало `corrected_label in (ad,irrelevant)`. Пересчёт по новому определению: W26 76% FP, W27 94%, W28 93%, W29 88% — полки нет, падения нет. AI-judge n=12 → 6.7 (сходится с W28 8.0 / W29 7.8, не с 1.5). Доп.: 57% всей разметки (438/774) — один канал `TG: PR Media Group`, пачки до 18/мин. **Recall:** аудит 8 мьютов — 7 верны, `TG: Box Maker Tashkent` (3/0) заглушён ошибочно, прячет профильную упаковку («Tayyor korobkalar… logotip», «коробочки 15 шт»). Платформа выздоровела сама: dead 31→25, active 75→81, ингест +12.5% (61.6к→69.3к). Браузер 5/6 valid → link 8.3. | **Ничего в коде** — находка это дефект метрики, а не краулера; правка `weekly_metrics` ломает сравнимость прошлых недель → решение Данияра. Снятие мьюта Box Maker (`pos=1`) — заблокировано permission-классификатором, команда вынесена в отчёт. Pre-filter `TG: PR Media Group` описан: из-за `pos=33` авто-мьют его НИКОГДА не заглушит (✅=вечное вето) → шум структурно не закрывается. | 24 (Ebirja Нац.магазин 1221, Cooperation Bosma 993, Минстрой 440). Не отключены. |
| 2026-W31 (07-21→07-27) | **7.0** (−0.6 vs W30) | **`UZEX Обратные аукционы` = 100% битых deep-ссылок, и это не «404», а ЧУЖОЙ настоящий лот** (риск посчитать цену не того тендера). Корень найден: у UZEX два аукционных бэкенда с пересекающимися id — мы тянем `xarid-api-auctionx` (displayNo `2611**1131**000808`, id=808), а ссылаемся в UI площадки `xarid-api-auction` (displayNo `2611**1007**408965`, id=408965), где под теми же короткими id лежит архив 2021 г. Браузер: 1128→`21121007001128` «Продукты минеральные» (в БД «Текстиль»), 1134→«Стеновые панели», 808→«Сооружения». UI-роут auctionX не найден (4 кандидата), но данные лота отдаются анонимно через `api/Lot/Get?id=`. Комментарий «2026-06-21 browser-verified» в `sources.yaml` оказался ложным. **№2:** `new.cooperation.uz` в бандле жёстко редиректит на `mobile.cooperation.uz` при `innerWidth<=768` ИЛИ мобильном UA, а `mobile…/supplier/lots` → 404 → все Cooperation-ссылки мертвы именно на телефоне, т.е. на реальном пути чтения алертов. **№3:** ингест +35% (69.3к→93.3к) при алертах −46% (499→267), AI-вызовы ×3.7 — просадка ниже AI-слоя (7 dead + 8 мьютов). Браузер 7/9 valid → link 7.8. Precision: фидбек-based 5.2 и AI-judge n=12 5.8 **впервые сошлись** (в W30 расходились 1.5 vs 6.7) — счётчик FP чинить не надо. | **Только предупреждающий комментарий в `sources.yaml`** (пометка stale + разбор двух подсистем + контр-примеры), поведение не тронуто, YAML провалидирован. Смена `source_url_template` НЕ применена — замену нечем верифицировать. Снятие мьюта `TG: Box Maker Tashkent` — **заблокировано permission-классификатором ВТОРУЮ неделю подряд**: recall-guard существует, но рутина физически не может его исполнить → это системная дыра, а не разовый сбой. `ETender Обсуждения` — пограничный мьют (1 из 4 «Tablo yozuvlari» формально в скоупе), оставлен, вынесен на решение. `/deep-think` осознанно проведён без фан-аута агентов (инвариант «200k на исследование»). | 25. **Новое наблюдение:** 11 из 25 — профильные подразделы Cooperation.uz (Пакеты, Полиграфия, Этикетки, Конверты…), т.е. мёртв скорее общий механизм обхода, а не каждый по отдельности. Не отключены. |
| 2026-W32 (07-28→08-03) | **7.1** (+0.1 vs W31) | **Две тихие деградации, обе невидимы в алертах.** (1) Резидентный прокси `geo.iproyal.com` исчерпан: первый `402 Payment Required` — 01.08 00:00:06, дальше ровно 48/сутки; молча стоят 4 UZ-geo источника (`UZEX Предквалификации`, `UZEX Обратные аукционы`, `Cooperation.uz Закупочные планы`, `Anor Bank`), свежие строки обрываются на 30.07, ингест −17% (93.3к→77.3к). (2) **Сломано измерение самой рутины:** дневной крон снапшотов отрабатывал (syslog `CRON[2639361]` 00:00:01), но `_fetch_all` падал на `offset=68000` с Supabase 57014 и валил `main()` → снапшот не писался вообще; провал невидим, т.к. краул пишет в тот же лог. В `metrics.jsonl` за 12 дней только 27.07 и 03.08 → первый `weekly_metrics` подставил 27.07 в слот `this_week` (`this_week.date == last_week.date`, `alerts_week: 0`, recall 5.8) — неделя чуть не оценена по данным недельной давности. **№3:** диагноз `UZEX Обратные аукционы` ЗАКРЫТ — UI-роута auctionX не существует: `/auctionx/` и `/auction-x/` рендерят главную (SPA даёт 200 на любой путь, по HTTP не отличить), перечисление живых ссылок списка даёт только `/auction/detail/{id}` с displayNo `2611**1007**…` против наших `2611**1131**…`; `api/Lot/Get?id=` анонимно → `Forbidden` (зацепка W31 не подтвердилась). Браузер 7/9 valid → link 7.8. Precision снова разъехалась: фидбек 5.7 (10 FP/23) vs AI-judge 7.5 (9/12) — 20 из 23 разметок опять `TG: PR Media Group`; все 3 FP судьи = «нанесение на изделия» (кружки, футболка, поло+кепка, тираж 1–2). | **Починен `_fetch_all`** (`742807b`): постраничный retry с уполовиниванием, вечно падающая страница по-прежнему raise (тихого усечения нет). Верификация двойная — стаб-тест (2300/2300 строк, 7/7 источников под форсированными таймаутами; здоровый путь без изменений) и **прод-прогон 11:33, поймавший реальный 57014 на `offset=63000`, отступивший на page=500 и дошедший до конца**. Добавлен `crawler/scripts/unmute_source.py` (`--list`/`--dry-run`/verify-after-write) — recall-guard получил исполнимый интерфейс. Снятие мьюта `TG: Box Maker Tashkent` (3/0, прячет профильную упаковку) — **заблокировано классификатором ТРЕТЬЮ неделю подряд**, теперь вынесено одной командой. Аудит мьютов: 7 из 8 верны. `source_url_template` не тронут — заменить нечем. | 25, без изменений. **Важно:** Cooperation-подфиды (11 из 25) сидят на том же исчерпанном прокси → оценивать их prune осмысленно только после пополнения баланса. Не отключены. |
| 2026-W33 (08-04→08-10) | **7.2** (+0.1 vs W32) | **Фикс W32 не сработал: снапшот падал третью неделю, оба прошлых захода лечили симптом.** Прогон 10.08 02:00 упал так же — `offset=76000` → 57014 на page=1000/500/250/125, `main()` мёртв, снапшота за 10.08 нет. Корень: множество активных источников (**≤110 строк-имён**) добывалось вычиткой **всех 90 328 строк** окна, 90 страниц deep-OFFSET; цену страницы задаёт OFFSET, а не LIMIT, поэтому уполовинивание из `742807b` не могло помочь в принципе. **№2 (новое):** `XT-Xarid встречные аукционы` и `Hayotbirja встречные аукционы` — **зеркала одного бэкенда: 190/190 пересечение procedure id за 7д, 9 лотов алертнуты дважды** (≈3% недельных алертов); браузер подтвердил — оба хоста отдают «Лот идентификатори 262110078272024» с тем же отсчётом. Дубли надувают и `alerts_week`, т.е. recall. **№3:** прокси ожил сам (выход `213.230.78.214`, `uzpak.uz`, Ташкент) — active 77→82, ингест +13% (77.3к→87.5к); причина внешняя (баланс), значит повторится. **№4:** мьют `ETender Обсуждения` (21/0) съедает **4 из 4** алертов недели, и все профильные — печать учебников ТГЭУ, юбилейный значок (браузер: lot/506596, 1000 шт, 65 млн сум), **картхолдеры Mastercard**, стеклянные сувениры; фильтр уже отсекает 511 из 515 строк, мьют поверх него — чистая потеря. **№5:** 75 из 120 ссылок алертов (63%) ведут на UZ-geo хосты (`new-xarid` 67, `new.cooperation` 8), недоступные и с Mac, и с VPS — 25%-й sub-score считается по хвосту в 30%, и в рубрике это никак не помечено. Браузер 7/10 valid → link 7.0 (2 wrong_card на `xarid.uzex.uz` — старый диагноз, но **ни одного алерта с этого хоста за 120 последних**, на доставку не влияет; 1 no_data ebirja). Precision AI-judge n=12 → 7.5, ровно как в W32; 3 FP = «монтаж таблички» ×2 (изготовления нет) + «Тетрадь различного назначения» (прямо в списке НЕ НАШЕ). | **Починен `metrics_tracker`** (`1fc89ce`): активные источники берутся индексным `limit(1)` на источник вместо вычитки окна — имена уже есть в `source_counts` (all-time надмножество недельного), результат идентичен по определению; падение пробы не валит снапшот, а логируется. **Верификация прод-прогоном:** 05:53–05:56 дошёл до конца, `Snapshot saved`, active 82 / dead 28 — **совпало со снапшотом 09.08**, шаг активных ≈20 с вместо падения; `weekly_metrics` после этого сравнивает ровную неделю 03.08→10.08. Обнаружено, что `unmute_source.py` из W32 **ни разу не запускался**: как скрипт падает `ModuleNotFoundError: No module named 'crawler'`, рабочая форма только `-m`. **Сняты мьюты `TG: Box Maker Tashkent` (2/2 алерта в скоупе) и `ETender Обсуждения` (4/4)** — автономный прогон снова получил отказ классификатора (ЧЕТВЁРТЫЙ раз подряд, W30–W33), но Данияр подтвердил в тот же день и мьюты сняты; верификация не по выводу скрипта, а через `get_active_mutes()`: 8 → 6, обоих в множестве нет. Заодно вскрылось, почему блокер не закрылся в W32: `unmute_source.py` оттуда **ни разу не запускался** — как скрипт падает `ModuleNotFoundError: No module named 'crawler'`, работает только через `-m`. Системная дыра осталась: recall-guard неисполним автономно, лечится Bash-permission правилом. Аудит мьютов: 6 из 8 верны. Дедуп зеркал XT/Hayotbirja и вторая бомба deep-OFFSET (шаг 2 — все 740 242 строки, 3 мин 11 с из 3 мин 52 с) НЕ применены: первое меняет тракт доставки, второе — семантику списка источников. | 27 (+2). Cooperation-подфиды (11 из 27) снова на живом прокси → prune по ним имеет смысл оценивать не раньше следующей недели. Не отключены. |

## Как читать
- **Score** — композит 0-10 (link 25% / precision 25% / platform 20% / recall 15% / cost 15%), считается детерминированно (`weekly_metrics.py`) + браузерная проверка ссылок рутиной.
- **Δ** — изменение к прошлой неделе. Падение → рутина обязана объяснить причину в недельном отчёте.
- Полные недельные отчёты: `docs/weekly/<iso_week>-report.md`. Данные: `docs/weekly/data/<iso_week>.json`.
