# Карточка приёмки C1–C5 и выпусков A–E — 24.09.2026

План: `docs/plans/2026-09-22-c1-c5-reliability-fixes.md`, Task 11. Выпуски A–E
слиты ночью 22.09 (PR #41–#49, последний код Codex — `3622894`). Приёмка
проведена 24.09 на проде `/opt/parsing-seo`; каждый PASS ниже — со ссылкой на
то, чем он подтверждён. Время — UTC.

**Итог:** контур **восстановлен** (ворота A–C пройдены, восстановление и поздний
detail проверены). Мониторинг **не принят**: ворота D ждут первого штатного
еженедельного цикла 28.09 04:00. Ворота E закрыты 24.09 вечером — строгий скан в расписании.

## Ворота

| | Сценарий | Ожидаемое | Фактическое | Доказательство | Вердикт |
|---|---|---|---|---|---|
| A | Полный API-обход после выкладки проходит сохранение | нет общего падения, строки записаны | до фикса: 22.09 14:00 и 16:00 — `upserted=0` (AttributeError db.py). С 22.09 18:16 — 26 обходов подряд без общего падения; записано 98.96–99.83% полученного (разница — дедупликация, отказов записи 0) | `crawl_runs` a5b096d0, 2b3fb269 (сбой); a5b5ec46 … 46e314dc (норма) | **PASS** |
| B | Лоты, потерявшие detail в окне сбоя, восстановлены | у каждого ID manifest — terminal outcome | manifest 70 ID (68 Xarid Конкурсы, 2 ETender UZEX): dry-run `would_update 70` → применено `updated 70` с backup → повторный проход `already_detail 70`, gaps 0 | `data/detail-recovery/20260922-exact-{dry-run,backup,applied}.json`, `20260922-two-pass-check.json` | **PASS** |
| C | Отказ источника виден, а не тонет в «пусто» | ошибка в журнале обхода и в healthcheck | отказы прокси (402) с 24.09 08:00 записаны в `crawl_runs.error_sources` каждого обхода (`cooperation-plans-filtered`, `uzex-auctions`, `uzex-prequest`); healthcheck видит следствия (`freshness.full_api`, `token.cooperation`) | `crawl_runs` dd39dab2, de641ecf, 9b0df380, 46e314dc; `parsing-seo-healthcheck.log` 12:15–13:15 | **PASS** (причину-прокси healthcheck не называет — см. ограничения) |
| D | Паспорт всех девяти бирж: свежо проверено или явный отказ | ни одна строка не молчит | ручной сбор 24.09 13:28: 6 строк рабочие или с известным пределом, 3 — явный `collector_error`. Ebirja tender/selection 422 «Type is invalid.» — API перестал принимать скалярный `type`; исправлено `6188f4d` (массив, как шлёт сайт), после выкладки — см. ниже. Cooperation — 402 прокси. Outbox пуст, state на 3 источника | `/root/comp-week-20260924-sources.json`; `data/competitor-award-monitor/{state,outbox}.json` | **ЧАСТИЧНО**: нет штатного weekly-цикла |
| E | Строгий shadow >20 млн с валютой и unknown | режим есть, цифры воспроизводимы | `--strict-competitor` в `shadow_search.py` (`5dddba6`), граница 20 000 000 / 20 000 001 и unknown покрыты тестами. С 24.09 — в еженедельном кроне (пн 03:40) отдельным шагом **после** общего скана: `--scan --audit-candidates --strict-competitor --judge-limit 4` (последовательно — скан перезаписывает состояние целиком). Боевой прогон 24.09 14:40: 10 с, EXIT 0, окно 10 319 лотов; audit-jurnal 7 новых >20 млн (in-scope 4/4), audit-gazeta 4 (3/4), остальные три — 0. Общий production gate не тронут | `crawler/tests/test_shadow_search.py`; `/root/shadow-strict-20260924.log`; crontab, бэкап `/root/crontab-backup-20260924-shadow.txt` | **PASS** (первый штатный — пн 28.09) |

## Пункты Task 11

1. **Объединённый набор тестов.** 857/857 на Mac в прямом и обратном порядке
   файлов (после `6188f4d`). Затронутые файлы повторены на VPS (Python 3.12.3) —
   см. ниже.
2. **Два штатных полных обхода после финального выпуска.** Есть с запасом:
   после `3622894` — 23.09 00:00 … 24.09 14:00, все с записью. Обход 24.09 00:00
   шёл 148 с при 9 ошибках источников (XT-Xarid, Hayotbirja — разовый сбой
   площадки, в 02:00 чисто). Известные ошибки не скрыты.
3. **Процесс обратной связи.** Опрос Telegram живой (HTTP 200), но healthcheck
   снова `STALE CODE`: код обновлялся 24.09 в 13:50–14:20 после рестарта в 11:20.
   Нужен штатный restart. TG-краулы 24.09 без ошибок (12:30: 22 записи, 3 алерта).
   Cooperation — регресс внешний: прокси без оплаты.
4. **Manifest восстановления закрыт** — ворота B.
5. **Настоящий weekly-цикл — PENDING.** Первый штатный запуск по крону —
   пн 28.09 04:00 (`run_competitor_award_weekly.sh`); до этого только ручные
   прогоны 21–22.09. Пункт live delivery не закрыт.
6. **Документы сверены** — `Projects/parsing-seo/changelog.md` и `main.md` в
   Second Brain.
7. **Эта карточка.**

## Проверка на VPS после выкладки `6188f4d` (24.09 14:20)

- Затронутые тесты на проде (Python 3.12.3): 48/48 — `test_ebirja_contract_api`,
  `test_competitor_crawl_completeness`, `test_keyword_varaqa`,
  `test_detail_text_persistence`, `test_shadow_search`, `test_rate_limiter`.
- Ebirja, сбор с 17.09 (read-only, публичный API): tender `complete=True`
  (`page_count_end`, 2 договора), selection `complete=True` (`date_boundary`,
  10), auction `complete=True` (20), shop `complete=True` (1093). До фикса
  tender и selection — `collector_error` 422.

## Остаточные ограничения

- **Прокси.** С обхода 24.09 08:00 резидентный прокси отвечает `402 Payment Required`
  (последняя успешная выгрузка через него — 03:39, в 09:30 выгрузка пропущена):
  стоят Cooperation (оферты, э-магазин, планы, контракты), UZEX обратные аукционы
  и предквалификации, Anor Bank. Healthcheck называет следствия, но не причину.
- **Невидимые победители.** XT-Xarid и Hayotbirja победителя не публикуют; это
  предел площадки, а не сбой. «Все победы всех бирж ловим» по-прежнему не
  утверждается.
- **Отступление от правила E.** Правило плана: слово в боевой фильтр — только
  после shadow (≥20 наблюдений или неделя). `варақаси/варақалар` добавлены
  24.09 (`b841c45`) напрямую по решению Данияра; замер — 3 совпадения за 14 дней
  на 169 849 строках, все печать. Выборка меньше требуемой.
- **Вне плана, найдено при приёмке.** Раздел etender ВМК-69 (`CivilContracts`)
  не собирался — подключён `c3480c1`; разбор в changelog 24.09.
